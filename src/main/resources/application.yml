spring:
  application:
    name: carDetect
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/car_detect
    username: root
    password: qr2kr6nu
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 300000
      max-lifetime: 1800000
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: false
redis:
  host: localhost
  port: 6379
  password:
  database: 0
  jedis:
  pool:
       max-active: 100
       max-idle: 10
       min-idle: 5
       max-wait: 30000
logging:
  level:
    org.springframework.jdbc.support.JdbcTransactionManager: debug
